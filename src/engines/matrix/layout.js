// Pure config — no imports, no circular dependencies
// The actual layout execution is handled by EngineRegistry (src/utils/layouts/engines/EngineRegistry.js)
export default {
    algorithm: 'matrix',
    isHorizontalFlow: true,
    edgeStyle: 'orthogonal_astar'
};
